<!-- <script>
    import { onMount } from 'svelte';

    let startX, startWidth;
    let width = 300;
    let resizing = false;
    let box;

    function resize(event) {
        width = startWidth + (event.clientX - startX);
    }
</script>

<div class="w-full h-full bg-green-700 " style = "width: {width}px" bind:this={box} on:mousedown={resize}></div> -->